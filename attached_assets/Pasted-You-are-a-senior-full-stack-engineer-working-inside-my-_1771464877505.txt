You are a senior full-stack engineer working inside my existing Replit repo.

CONTEXT / CONSTRAINTS
- Keep the existing stack: Vite + React + TypeScript + Wouter, TanStack React Query, shadcn/ui + Radix, Tailwind; Express + TypeScript backend; Drizzle ORM + Postgres. Do NOT migrate to Next.js. (See existing package.json + tech stack docs.)
- Follow existing auth and entitlement patterns (isAuthenticated + requireEntitlement middleware). Never rely on frontend-only for access control.
- Avoid breaking existing Course 1 chat and existing Course 2 journaling/export features.

MAIN GOAL
Make the system “breezy and fun daily” by restructuring around:
Plan flow (Vision → Month → Habits → Eisenhower) and then a Today-first daily ritual.
Add 3 one-tap quick tools (Containment, Micro Movement, Self-Compassion), voice-to-text input, and timers with a real audible alarm.

REQUIRED FEATURES (IMPLEMENT ALL)

A) INFORMATION ARCHITECTURE / NAV
1) Create a minimal app shell nav:
   - Today
   - Plan
   - History
   - Tools
2) After login, default route should land on Today (NOT a calendar/toolbox).
3) Keep Course 1 unchanged. Course 2 journaling/calendar moves under History (don’t remove it; re-home it).

B) TODAY PAGE (DAILY LOOP)
Today must load and show:
- Due habits (only those due today)
- Top 3 tasks for today
- Scheduled Q2 blocks for today (from Eisenhower)
- Quick access tools section (3 one-tap tools)
- Journal quick entry access (morning/evening quick mode with expand for deeper)

Quality requirements:
- Auto-save on text changes (debounced) with “Saving…” → “Saved ✓”
- Minimal scroll, minimal decisions
- Clean empty states + skeleton loaders

C) PLAN WIZARD (ORDER MUST MATCH)
Create PlanWizard with stepper and “Save & continue later”:
1) Vision (10-year vision prompt, short)
2) 1-month plan (choose focus, metric, 3 weekly outcomes)
3) Habits setup (max 5 active habits)
4) Eisenhower weekly plan setup

Use progressive disclosure:
- Quick Mode by default (short prompts)
- Expandable “Why this matters” sections

D) HABITS IMPROVEMENTS
1) Habits must be duplicate/copyable:
   - Add “Duplicate” action on habit cards.
   - Duplicate should create a new habit and open edit immediately.
   - If user already has 5 active habits, create duplicate inactive + toast message.
2) Recurrence must support ONLY:
   - Weekly (every 1 week)
   - Every X weeks (2–12)
   Plus day(s) of week selection and a “Next 3 occurrences” preview.
3) Today page must show ONLY habits due today.

E) EISENHOWER WIZARD + DRAG/DROP SCHEDULING
1) Eisenhower should not start as a blank matrix.
2) Weekly planning wizard flow:
   - Select week start + roles (default Health/Wealth/Relationships, editable).
   - Big rocks first: 1–3 Q2 outcomes per role (auto quadrant = Q2).
   - Brain dump inbox (one per line).
   - Guided classification: for each item ask:
     a) Important? (values/goals/prevent real problem)
     b) Urgent? (deadline/penalty within 48 hours)
     c) Optional: “Is this someone else’s urgency?” for Q3 nudge
     Auto-suggest Q1/Q2/Q3/Q4, user can override.
3) Scheduling screen:
   - Show unscheduled Q2 list + weekly calendar grid (Mon–Sun, time slots).
   - Implement drag & drop from Q2 list onto calendar to set date/time/duration.
   - Persist scheduledDate, scheduledStartTime, durationMinutes.
   - Mobile fallback: tap item → pick day/time modal.
4) Add optional “Auto-schedule Q2” button (simple heuristics).
5) Use a lightweight drag/drop library (dnd-kit).

F) QUICK ACCESS TOOLS (ONE-TAP) — ALWAYS ACCESSIBLE FROM TODAY
Add a “Quick tools” card with 3 buttons. Each opens a modal/flow:

1) Emotional Containment (one tap)
   - Steps: FEEL (10–20s timer) → NAME (sentence template) → REGULATE (timer for slow exhales) → MOVE (choose tiny action)
   - Must include timers, and when done offer “Add MOVE action to Today’s tasks”
2) Micro Movement (one tap)
   - Default: 2-minute timer + alarm
   - Provide a few suggested movements (walk 2 min, stretch, arm raises, wash dishes)
3) Self-compassion (one tap)
   - Prompts:
     a) “What’s going on?” (optional)
     b) “What would you say to a loved one?”
     c) “Now say that to yourself as that loved one.”
   - Offer “Save to today’s journal” (append to notes)

G) VOICE INPUT (VOICE-TO-TEXT)
Add voice input to major text fields (journaling, planning, quick tools).
Implementation:
- Primary: Web SpeechRecognition if available.
- Fallback: record audio via MediaRecorder and POST to /api/audio/transcribe (multipart form data).
Backend:
- Add /api/audio/transcribe route, protected by isAuthenticated + course2 entitlement.
- Use OpenAI audio transcription via existing OpenAI client configuration and environment variables.
- Validate file size and mimetype; delete temp files after processing.

H) TIMERS + REAL ALARM
- Build reusable Timer component/hook with Start/Pause/Reset.
- When timer hits 0, play an audible alarm sound that reliably fires:
  - Use Web Audio API beep and “unlock” audio context on Start click.
- Optionally vibrate and show a notification if permission already granted (don’t nag).

I) DEPENDENCIES / PACKAGE.JSON
Update package.json dependencies if missing:
- multer (+ @types/multer) for file upload
- @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities for drag/drop
If adding any dependency, ensure imports compile.

J) DB / SCHEMA
Inspect current Drizzle schema and add/extend tables/columns needed for:
- habits recurrence (intervalWeeks, daysOfWeek, startDate, active)
- habit completions (if not present)
- eisenhower entries (quadrant, weekStart, role, title, scheduledDate, scheduledStartTime, durationMinutes)
Keep backwards compatibility: do not break existing data; use defaults/nullables if needed.
After schema change, ensure instructions mention running `npm run db:push`.

K) QUALITY / FINAL OUTPUT
- Keep UI consistent with shadcn/ui patterns.
- Add loading skeletons and empty states.
- Use React Query for fetching/mutations with optimistic updates where appropriate.
- At the end, output:
  1) A list of files changed/added
  2) Commands I must run (npm install, db:push, check, dev)
  3) A short smoke-test checklist (Today, Plan wizard, voice input, timers alarm, Eisenhower drag-drop).

Now implement this end-to-end in the repo with minimal disruption to existing routes.