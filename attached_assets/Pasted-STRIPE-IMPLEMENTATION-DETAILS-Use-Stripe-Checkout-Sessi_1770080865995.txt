STRIPE IMPLEMENTATION DETAILS:
- Use Stripe Checkout Sessions:
  - mode="payment" for 30-day pass
  - mode="subscription" for membership
- Store Stripe price IDs in env vars:
  - STRIPE_PRICE_PASS_30D
  - STRIPE_PRICE_SUB_MONTHLY
- Create /api/stripe/create-checkout-session that uses logged-in user id and returns checkout url.
- Implement /api/stripe/webhook:
  - Verify signature using STRIPE_WEBHOOK_SECRET and raw body.
  - Handle checkout.session.completed:
    - If payment mode: set access.access_until = now + 30 days; mark order paid; create or update access record.
    - If subscription mode: set subscription_status active; store stripe_subscription_id/customer_id; set subscription_current_period_end; mark order paid.
  - Handle checkout.session.expired -> mark order expired
  - Handle invoice.payment_failed -> set subscription_status past_due (lock access)
  - Handle customer.subscription.deleted -> set subscription_status canceled (lock after period end)
  - Handle charge.refunded or checkout.session.completed with payment_status != paid -> mark refunded and revoke pass access
- Add idempotency: never grant twice for same stripe_session_id; use unique constraint.
- Add /app/billing “Refresh access” button:
  - Calls /api/billing/refresh which looks up the user’s most recent order/session, retrieves session/subscription from Stripe, and re-runs the same grant logic safely (idempotent).
  - This addresses “I paid but it didn’t unlock”.

OPENAI / GPT IMPLEMENTATION:
- Use OpenAI Responses API from the server route /api/agent
- Enforce access entitlement BEFORE calling OpenAI.
- Enforce rate limits AND per-user daily/monthly caps:
  - Example: max 40 messages/day for pass users; max 200/day for subscribers (make configurable)
  - Track usage_daily table
- Token caps:
  - Set max_output_tokens (e.g. 800–1200) and truncate conversation history to a max context window.
- Build 3 system prompts (Lesson 1/2/3) that guide the user step-by-step and save outputs (direction_summary etc).
- Never expose OPENAI_API_KEY to client. All calls are server-side.

EMAIL FLOWS:
- Auth emails handled by Clerk email template customization (welcome/verify/reset).
- Stripe receipts are handled by Stripe settings.
- Add a transactional “Access granted” email after webhook fulfillment using Resend (or similar). Also add “Access expiring soon” email (optional cron later).
- Implement a basic /api/email/send with Resend and a React email template.

DEVELOPER EXPERIENCE:
- Provide a README.md with:
  - required environment variables
  - how to run dev
  - how to set Stripe webhook locally (Stripe CLI instructions)
  - test card numbers for Stripe test mode
- Provide a /health route that checks DB + Stripe + OpenAI connectivity and reports missing env vars (without printing secrets).
- Add basic Playwright tests for:
  - unauthenticated access blocked
  - unpaid user blocked from /app/lesson-1
  - paid user allowed
  - agent endpoint returns 401/403 when locked

ENV VARS (Replit Secrets):
OPENAI_API_KEY
STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET
STRIPE_PRICE_PASS_30D
STRIPE_PRICE_SUB_MONTHLY
STRIPE_PORTAL_CONFIGURATION_ID (optional)
CLERK_PUBLISHABLE_KEY
CLERK_SECRET_KEY
NEXT_PUBLIC_APP_URL
RESEND_API_KEY
FROM_EMAIL
ADMIN_EMAILS
UPSTASH_REDIS_REST_URL (optional)
UPSTASH_REDIS_REST_TOKEN (optional)
TURNSTILE_SITE_KEY (optional)
TURNSTILE_SECRET_KEY (optional)
