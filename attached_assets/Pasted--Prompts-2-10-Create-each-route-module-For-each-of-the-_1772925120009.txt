## Prompts 2-10: Create each route module

For each of the following, tell Replit:

```
I'm splitting server/routes.ts into modules. Create a new file server/routes/billing.ts that contains ONLY the billing-related routes from routes.ts. This includes:
- GET /api/purchases
- POST /api/checkout
- POST /api/stripe/webhook
- POST /api/billing/refresh

Wrap them in an exported function called registerBillingRoutes(app: Express).
Import storage from "../storage", isAuthenticated from "../replit_integrations/auth", getUncachableStripeClient from "../stripeClient", COURSES from "@shared/schema", checkoutSchema from "../validation", and AuthRequest from "./helpers".
Use parseId and parseDateParam from "./helpers" for any param validation.
Keep all the existing logic exactly the same, just move it into this new file.
```

Then repeat for each module:

```
Create server/routes/journals.ts with an exported registerJournalRoutes function containing:
- GET /api/journals
- GET /api/journals/:date/:session
- POST /api/journals
- GET /api/journals/export
Import parseDateParam and exportRateLimit from "./helpers". Keep all existing logic the same.
```

```
Create server/routes/chat.ts with an exported registerChatRoutes function containing:
- GET /api/chat/messages
- POST /api/chat/messages
- POST /api/audio/transcribe
Move the OpenAI client, SELF_DISCOVERY_SYSTEM_PROMPT, and multer upload config into this file.
Import aiRateLimit and transcriptionRateLimit from "./helpers". Keep all existing logic the same.
```

```
Create server/routes/eisenhower.ts with an exported registerEisenhowerRoutes function containing:
- GET /api/eisenhower
- GET /api/eisenhower/week/:weekStart
- POST /api/eisenhower
- PATCH /api/eisenhower/:id
- DELETE /api/eisenhower/:id
- POST /api/eisenhower/reorder
- GET /api/eisenhower/export
- POST /api/eisenhower/parse-tasks
Move the OpenAI client into this file for the parse-tasks endpoint.
Import parseId, parseDateParam, csvEscape, aiRateLimit, exportRateLimit from "./helpers". Also add a local Zod reorderSchema for validating the reorder payload. Keep all existing logic the same.
```

```
Create server/routes/empathy.ts with an exported registerEmpathyRoutes function containing:
- GET /api/empathy
- POST /api/empathy
- PATCH /api/empathy/:id
- DELETE /api/empathy/:id
- GET /api/empathy/export
Import parseId, csvEscape, exportRateLimit from "./helpers". Keep all existing logic the same.
```

```
Create server/routes/habits.ts with an exported registerHabitRoutes function containing:
- GET /api/habits
- POST /api/habits
- PATCH /api/habits/:id
- DELETE /api/habits/:id
- POST /api/habits/reorder
- GET /api/habit-completions/range/:startDate/:endDate
- GET /api/habit-completions/:date
- POST /api/habit-completions
- PATCH /api/habit-completions/:habitId/:date
- DELETE /api/habit-completions/:habitId/:date
Import parseId, parseDateParam from "./helpers". Add a local Zod reorderSchema. Keep all existing logic the same.
```

```
Create server/routes/tasks.ts with an exported registerTaskRoutes function containing:
- GET /api/tasks
- GET /api/tasks/date/:date
- POST /api/tasks
- PATCH /api/tasks/:id
- DELETE /api/tasks/:id
Import parseId, parseDateParam from "./helpers". Keep all existing logic the same.
```

```
Create server/routes/identity.ts with an exported registerIdentityRoutes function containing:
- GET /api/identity-document
- PUT /api/identity-document
- PUT /api/vision-board
- GET /api/monthly-goal
- PUT /api/monthly-goal
- GET /api/plan-versions
- POST /api/plan-versions/save
- POST /api/plan-versions/:id/restore
- POST /api/plan-versions/clear
- DELETE /api/plan-versions/:id
- GET /api/meditation-insights
- POST /api/meditation-insights
- DELETE /api/meditation-insights/:id
Import parseId from "./helpers". Keep all existing logic the same.
```

```
Create server/routes/tools.ts with an exported registerToolRoutes function containing:
- GET /api/tool-usage
- POST /api/tool-usage
- PATCH /api/tool-usage/:id
- GET /api/tool-usage/export
- GET /api/custom-tools
- POST /api/custom-tools
- PATCH /api/custom-tools/:id
- DELETE /api/custom-tools/:id
- GET /api/trigger-logs
- POST /api/trigger-logs
- GET /api/avoidance-logs
- POST /api/avoidance-logs
Import parseId, csvEscape, exportRateLimit from "./helpers". Keep all existing logic the same.
```

```
Create server/routes/phase3.ts with an exported registerPhase3Routes function containing:
- POST /api/phase3/analyze
Move an OpenAI client into this file. Import aiRateLimit from "./helpers". Keep all existing logic the same.
```