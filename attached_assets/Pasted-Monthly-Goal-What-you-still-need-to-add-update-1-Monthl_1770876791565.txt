Monthly Goal: What you still need to add/update
1) Monthly Goal as a first-class object everywhere
Add/update
•	A single “Current Monthly Goal” source of truth (even if it’s localStorage for now)
o	keyed by YYYY-MM
o	fields:
	goalStatement (required)
	successMarker (optional but recommended)
	value (user-selected; ties to identity)
	why (optional)
	nextConcreteStep (required)
	prize (optional)
•	“Edit / Rewrite” behavior (not just “create once”)
o	your doc explicitly says “review daily” and “rewrite the goal” — your UX should make rewriting feel normal, not like “I failed.” 
How to set a Goal
Why this matters
If the goal isn’t globally available, you’ll get “Plan has a goal but Dashboard/Journal doesn’t,” and the system stops feeling coherent.
________________________________________
2) Library/Supporting Docs: the Monthly Goal doc must be present + usable
Add/update
•	Add the doc “How to Set a Real Goal” in Library / Supporting Docs as a card:
o	Open Guide
o	Start / Edit Monthly Goal
•	Add a condensed “Key Rules” view (one screen) extracted from the doc:
o	“Precision creates direction”
o	“One goal”
o	“One month”
o	“Review daily”
o	“Does this block my goal?”
o	“Next concrete step”
o	“Begin — action beats planning”
o	“Set a prize” 
How to set a Goal
Important editorial tweak (high ROI)
•	Make a client-facing version of the guide (same principles, slightly more universal language) while keeping your original doc available if you want.
This prevents a subset of users from feeling “this isn’t for me” when they read certain examples. 
How to set a Goal
________________________________________
3) /plan: Monthly Goal must be step 1, not “optional”
You said “it should all start with a monthly goal.” Great — your Plan experience should enforce that gently.
Add/update
•	In /plan:
o	Step 1: Monthly Goal
o	Step 2: Weekly Eisenhower/Q2 outcomes
o	Step 3: Habits for the week
•	If the user tries to plan the week with no monthly goal:
o	show an inline blocker: “Set your Monthly Goal first (3 min)”
o	do not let them skip silently
Monthly Goal form: enforce precision without friction
Add lightweight structure:
•	Goal Statement (required)
•	Success Marker (optional)
•	Next Concrete Step (required) ← this is your “action beats planning” bridge 
How to set a Goal
•	Prize (optional) ← motivation that isn’t points 
How to set a Goal
________________________________________
4) Weekly Eisenhower items must “cash out” into the monthly goal
Add/update (this is key)
When creating weekly Q2 outcomes/items, add:
•	Alignment field (optional):
“Supports my Monthly Goal because: ____” (1 sentence)
Add/update: the “Does this block my goal?” triage
Your goal doc says: If it doesn’t block my goal, ignore it and move forward. 
How to set a Goal
So in the UI where users add tasks / issues / problems, add one toggle/question:
•	“Does this block my goal?”
o	Yes → it becomes:
	a scheduled block, or
	a “blocking task” surfaced on Today
o	No → it becomes:
	“Later / Not now” list (not front-and-center)
This is how you prevent your task system from becoming a guilt machine.
________________________________________
5) Habits must be explicitly tied to the monthly goal (or intentionally not)
Add/update
In your “pick habits for the week” UI:
•	Force users to label each habit as one of:
o	Goal Habit (moves the monthly goal)
o	Learning Habit (learnind and self-development)
o	Maintenance Habit (keeps life stable)
This prevents shame loops:
•	missing a maintenance habit is not interpreted as “I failed my purpose”
•	users can still “win the day” by doing one goal habit + one small check-in
Also add this guardrail
•	Limit visible “daily habits” on the dashboard to 3–7 max (user-selected)
•	Everything else is hidden under “More habits”
This reduces friction and increases adoption.
________________________________________
6) Dashboard: Monthly Goal must be visible and operational
This is where Monthly Goal becomes felt usefulness, not just planning.
Add/update these dashboard elements
•	Monthly Goal Card (top third of the page)
o	Goal statement + success marker
o	“Today’s smallest move toward this goal: ____”
o	“Edit / Rewrite” + “Open goal guide”
•	Today Focus uses the monthly goal
o	If there’s a scheduled Q2 block today that links to the goal → show it prominently
o	If not → show a “Pick today’s smallest move” quick selector
•	Daily Goal Review (10 seconds)
o	“Is this still the goal?” (Yes / Adjust)
o	“Next concrete step?” (prefilled from Plan; editable)
This is exactly what your doc recommends: review daily + rewrite + visual cue. 
How to set a Goal
Add/update “re-entry” behavior (no shame)
If user hasn’t engaged in >3 days:
•	show “Welcome back” message
•	default CTA becomes “Pick the next concrete step (30 seconds)”
•	do not show missed streaks or “you’re behind”
This is how you prevent the streak-shame failure spiral.
________________________________________
7) Journaling: include ONE goal-aligned prompt (no compliance journaling)
Your journaling already has strong identity/value mechanics. The risk is adding too many “required” fields and causing junk journaling.
Add/update
In Quick mode:
•	Add one single line:
o	“Smallest action today that supports my Monthly Goal: ____”
•	Keep the rest minimal.
In Deep mode:
•	Goal can appear as contextual text (“This month you’re focused on: ___”)
•	But don’t make it another mandatory section.
This preserves quality: people aren’t rewarded for volume.
________________________________________
8) Coach/GPT area: inject Monthly Goal as context (without turning it into nagging)
Even if you aren’t “doing payments yet,” this is purely UX/outcome.
Add/update
•	When user opens GPT/Coach, show:
o	“Your Monthly Goal: ____”
o	“Next step: ____”
•	Provide 3 quick buttons:
o	“Help me choose today’s smallest move”
o	“Help me remove what’s blocking my goal”
o	“Help me rewrite my goal to be more precise” 
How to set a Goal
This makes the AI feel immediately useful (and aligned), without gamification.
________________________________________
9) Data + backend: minimal vs durable
If you’re currently storing monthly goal in localStorage, that’s fine — but here are the items you’ll need to add when you make it durable.
Add/update (when you move to DB)
•	monthly_goals table:
o	user_id, month_key, goal_statement, success_marker, value, why, next_concrete_step, prize, timestamps
•	Optional monthly_goal_revisions:
o	stores rewrites without shame (“rewriting is normal”)
If you don’t want new tables yet, at least store these shapes consistently in one place so you can migrate later.
________________________________________
Non-monthly-goal updates you should still make (even if not “connected”)
These are short, but they matter for the system you’re building:
A) One unified “Document Viewer” component
You now have multiple supporting docs (goal setting, meditation, containment, identity, etc.). Don’t build each as a one-off page.
Add/update:
•	a single DocViewer component that supports:
o	headings
o	“Key Takeaways” at top
o	“Start now” CTA (deep link to the relevant tool/flow)
This reduces friction and makes the library actually used.
B) Ensure “Today” is the default and everything else is one click away
Your IA goal is right:
•	videos → Learn
•	GPTs → Coach
•	weekly setup → Plan
•	calendar journaling → Journal
•	docs → Library
•	exercises → Tools
Just ensure:
•	post-login lands on Today dashboard, not course list
•	all other sections are always accessible from persistent nav
